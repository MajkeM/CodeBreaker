{
  "name": "renjs-vn-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173",
    "start": "npx live-server --port=5173 --open=./index.html",
    "fetch-renjs": "node ./scripts/fetch-renjs.js"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
